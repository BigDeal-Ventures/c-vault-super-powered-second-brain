# CLAUDE.md

This repository builds C-Vault - Super Powered Second Brain.

Use `packs/*` as source of truth and regenerate adapters with:

```bash
npm run generate
```

Do not publish private vault notes, client work, local API keys, `.obsidian/workspace.json`, backup folders, or machine-specific paths.
